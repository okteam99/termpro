# 待规划需求池(PENDING)

> 跨 Feature/session 的"范围外但要做"项 · 等用户拍板启动 · 只留 active(📝/🔄)· 转化即从表删。

| ID | 标题 | 来源 | 目标项目 | 背景(1-3 句) | 状态 | 加入日期 |
|---|---|---|---|---|---|---|
| PENDING-001 | 迁移边界健壮性组(BL-001 F4/F8) | BL-001 review defer | OKWORK | 部分迁移+fallback 删除+重试的已删复活;畸形 v1 存档(非数组/单条坏条目)致 hydrate 崩溃或永卡 v1 无诊断。低概率窄触发,一组做:数组守卫+单条容错跳过+坏条目上报 | 📝 | 2026-07-10 |
| PENDING-002 | Workspace 注册表周边小项组(BL-001 F6/F9/F11/F13 · F10 已并入 BL-004) | BL-001 review defer | OKWORK | 备份内容断言缺失(F6);remove/update no-op 仍广播 churn(F9);viewer 广播冗余(F11);注册表重试耗尽提示措辞(F13)。F10(service 边界 params 校验)已并入 BL-004 AC-9(远程面真耦合)。余 4 项均 MINOR/NIT · F11/F13 判据不可测需先改判据 · 单开 | 📝 | 2026-07-10 |
| PENDING-003 | WS 安全纵深与 token 运维面组(BL-002 F6/F7/F9) | BL-002 review defer | OKWORK | Origin 校验纵深(token 熵仍是真屏障);认证失败告警节流;token 交接运维面(stdout 落盘风险/token-file TOCTOU)。归 BL-003 部署流程一并设计 | 📝 | 2026-07-10 |
| PENDING-004 | WS 边界补测与门控细节组(BL-002 F8/F11/F12) | BL-002 review defer | OKWORK | 恰好 32MiB 帧/逐字节慢速握手/pong 迟到/握手风暴补测;门控实现细节(非数字 id 边缘/done-flip 时序/连接数上限→BL-003/005);poke 临时文件清理等 info 项 | 📝 | 2026-07-10 |
| PENDING-005 | 远程 workspace 独立查看器窗口可见性(BL-004 D-7 延后) | BL-004 goal PL-5/ARCH-3 defer | OKWORK | 独立文件/Diff BrowserWindow 各自持本窗口 hostRegistry 单例·无远程 client(token 依 BL-003 E8 只推主窗口);远程 workspace 看文件内容/Diff 需跨窗口远程访问=重开 E8 跨窗口 token 安全面。收窄了上游 WS-01-S4 AC②「任一客户端可见」/BL-001 多客户端定义,授权延后 | 📝 | 2026-07-10 |
| PENDING-007 | 终端链接高亮段尾宽字符(CJK)decoration 短 1 列(B260710093647 A5) | B260710093647 review 记录点(既有) | OKWORK | `rowSegments` 的 `width=endCol-startCol+1` 取末 code-unit 的 col,段尾宽字符第二格未计入 → 蓝色高亮短 1 格。仅装饰视觉,链接命中/点击不受影响(range/activate 独立)。修法:lineToString/rowSegments 按 `cell.getWidth()` 累加真实列宽 | 📝 | 2026-07-10 |
| PENDING-008 | 会话收养剩余边缘加固组(收养评审 P3-2/4 defer) | 1a30ddb opus review defer | OKWORK | 剩余:① path② attach found=false 留「幽灵 tab」且可能落焦(挂载即 new spawn,可接受降级,更净做法=found=false 回收该 tab);② 「真 readoptHost 串行两轮只建一份 tab」端到端属性无单一断言(现拆散在 T-036 与串行化用例)。原 P3-3「在途收养 generation 作废」已由 OKWORK-B260821031119 交付 | 📝 | 2026-07-12 |
| PENDING-009 | reap 前置单调闸 + 隧道 error 路径关闭(升级收养评审 P3 defer) | 升级收养修复 opus review defer | OKWORK | ① 降级客户端对协议不兼容的更高版活 host:先 reap 再被 deployBlockedByNewerVersion 拦下 → host 已死部署又失败(既有边,非回归)。加固:reap 前查 listReadyVersions,远端有更高 .ready 则不 kill 直接判 incompatible;并补该序的表征测试;② orchestrator.buildTunnel error 分支 reject 未 server.close(),罕见触发的 net.Server 泄漏(pre-existing) | 📝 | 2026-07-12 |
| PENDING-010 | 远程 tab 布局持久化边缘加固组(布局评审 P3 defer) | 远程 tab 布局里程碑 opus review defer | OKWORK | ① respawn 后死 sessionId 的 ptyListeners 条目不退订(有界·惰性无害·既有模式同弊);② 删除机器配置后其布局条目成孤儿永久随存档重写(dropHostWorkspaces 快照永不被 consume)——删机时按 hostId 清 remoteTabLayouts;③ consumeRemoteTabLayouts 一次性取走全部,首拉 workspace.list 不完整时缺席 ws 的布局被当「已删」丢弃(实践低风险:注册表同步加载先于 serving)——未匹配条目可回存待 workspace:changed;补测:早到 pty:data 双写不变式 / 单机多 ws 多条目 restore | 📝 | 2026-07-12 |
| PENDING-012 | 多端同屏收尾评审 P3 组(镜像 kill 语义/身份文件清理/exit 后订阅集卫生) | 多端同屏里程碑 opus review defer | OKWORK | ① pty.kill 门控=订阅者集合,任一设备关 tab 会为所有设备终止共享会话(单租户信任边界内但 UX 意外)——多订阅时降级 unsubscribe 或二次确认(归 M3);② SIGTERM 只清端口文件,identity/<tag>/token 残留(新 host 原子覆盖、死 host probe 必败,功能无害,可一并 best-effort unlink);③ 会话 exit 只回调 spawn 方 client.sessions.delete,镜像端残留死 sid(input 被 exited 挡、close unsubscribe no-op,纯状态卫生);④ M3 遗留:tab 名称/顺序服务端化 + 完整「点击取回」UI + 收敛后自动清理本设备旧 configId host;⑤ mkdirLock 陈旧判定跨机时钟偏差 enabler(P1-1 已封杀害路径,锁语义可后续改服务端时钟基准) | 📝 | 2026-07-13 |
| PENDING-013 | BL-008 登录连续性回归门禁补齐组(pm_acceptance F1/F2/F3 defer) | BL-008 pm_acceptance defer(用户拍板 approved_and_ship) | OKWORK | ① AC-4 的设备侧容量淘汰抑制 `profileContinuityController.ts:1249` `if (cause === 'evicted') return;` 零测试触达(TC 的 T-006 测的是 Host schema 拒绝一个真实系统不会发出的 RPC)——补 `cookieStore` emit `cause='evicted'` → 断言零 push、不产生 tombstone;② AC-5 的 `host_upgrade` 提示链路零测试(`describeContinuity()` 抛 `PROFILE_STORAGE_INCOMPATIBLE` → `reasonOf():209` → `state:'host_upgrade':1416` → UI 文案 `BrowserProfilesSection.tsx:60`,UI 测试只跑过 attention/paused/synced 三态)——补 provider 抛错 → 断言 state + 升级提示文案;③ TC.md 三处 test_refs 指向错位(AC-1/T-002、AC-7/T-012、AC-10/T-016),真证据在未被引用的 `browserGuestNavigationGuard.test.ts` / `profileContinuityJournal.test.ts:200` / `remoteProfileAuthority.test.ts:1106`+`remoteProfileStore.test.ts:502`——改指向。**均为缺回归门禁非当前缺陷**(两条实现链路已读码走通、正确) | 📝 | 2026-08-13 |
| PENDING-014 | Browser Profiles 行内 `Change location` 按钮被弹窗右缘裁切(BL-007 引入 · BL-008 pm_acceptance 观察到) | BL-008 pm_acceptance 视觉核对 | OKWORK | `browser-profiles__row` 是无 wrap 的 flex,只有 `__row-desc` 可收缩(`BrowserProfilesSection.css:83-89,117-133,142-150`);520px 设置弹窗内 name+badge+storage+password-count+actions 撑满后,User-Agent 描述被挤到零宽、`Change location` 按钮被裁(见 `real-app-browser-settings.png`)。`__storage` 由 BL-007 `7271da0` 引入,BL-008 新增均为行外新块,非 BL-008 回归 | 📝 | 2026-08-13 |
| PENDING-015 | 子浏览器窗口(OAuth 弹窗)未接入 continuity hydration gate | BL-008 ship1 合并 origin/main 时发现 | OKWORK | main 的 `e1390ff`(弹窗开子浏览器窗口)与 BL-008 的 fail-closed hydration gate 是**并行开发、合并时才相遇**的两个特性。子窗 `BROWSER_POPUP_WINDOW_OPTIONS`(`main.ts:2189-2201`)不设 partition → **继承开启方 guest 的 session/分区**(OAuth 共享登录态的前提),而 `adoptBrowserPopupWindow` 的 `will-navigate` 只查 scheme(`main.ts:~2306`),没有像 webview guest 那样接 `createBrowserGuestNavigationGuard`。后果:Remote Profile 在 Host generation 失效/离线时,OAuth 弹窗可在 hydration 完成前发出网站请求——即 ADR-0004 决策 4 与约束「新增导航入口必须接入同一 main-side gate」未覆盖的入口,和 review 抓到的 F1 同族。修法:`adoptBrowserPopupWindow` 复用开启方的 guestBinding 装同一 guard(子窗无地址栏,阻断后只能靠受控 replay),并补一条子窗导航的确定性测试 | 📝 | 2026-08-13 |
